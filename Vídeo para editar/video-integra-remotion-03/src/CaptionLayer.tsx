import {createTikTokStyleCaptions,type Caption,type TikTokPage} from "@remotion/captions";
import {useCallback,useEffect,useMemo,useState} from "react";
import {AbsoluteFill,Easing,interpolate,Sequence,staticFile,useCurrentFrame,useDelayRender,useVideoConfig} from "remotion";

const PAGE_MS=1050;
const Page:React.FC<{page:TikTokPage}>=({page})=>{const frame=useCurrentFrame();const{fps}=useVideoConfig();const time=page.startMs+frame/fps*1000;return <AbsoluteFill style={{justifyContent:"flex-end",alignItems:"center",paddingBottom:590}}><div style={{maxWidth:1260,padding:"17px 28px 20px",borderRadius:22,background:"rgba(5,22,33,.87)",boxShadow:"0 12px 38px rgba(0,0,0,.32)",color:"white",fontFamily:"Arial,sans-serif",fontSize:52,lineHeight:1.08,fontWeight:900,textAlign:"center",textTransform:"uppercase",opacity:interpolate(frame,[0,4],[0,1],{extrapolateRight:"clamp"}),translate:interpolate(frame,[0,5],["0px 16px","0px 0px"],{easing:Easing.bezier(.16,1,.3,1),extrapolateRight:"clamp"})}}>{page.tokens.map(t=>{const active=t.fromMs<=time&&t.toMs>time;return <span key={`${t.fromMs}-${t.text}`} style={{color:"white",background:active?"#087A68":"transparent",borderRadius:active?8:0,padding:active?"1px 5px 2px":0}}>{t.text}</span>})}</div></AbsoluteFill>};

export const CaptionLayer:React.FC=()=>{const[captions,setCaptions]=useState<Caption[]|null>(null);const{delayRender,continueRender,cancelRender}=useDelayRender();const[handle]=useState(()=>delayRender("Legendas"));const load=useCallback(async()=>{try{const r=await fetch(staticFile("captions.json"));setCaptions(await r.json());continueRender(handle);}catch(e){cancelRender(e);}},[cancelRender,continueRender,handle]);useEffect(()=>{load();},[load]);const pages=useMemo(()=>captions?createTikTokStyleCaptions({captions,combineTokensWithinMilliseconds:PAGE_MS}).pages:[],[captions]);const{fps}=useVideoConfig();if(!captions)return null;return <AbsoluteFill>{pages.map((p,i)=>{const next=pages[i+1];const start=Math.floor(p.startMs/1000*fps);const last=p.tokens[p.tokens.length-1];const end=Math.ceil((next?next.startMs:(last?.toMs??p.startMs+PAGE_MS))/1000*fps);return end>start?<Sequence key={`${p.startMs}-${i}`} from={start} durationInFrames={end-start}><Page page={p}/></Sequence>:null;})}</AbsoluteFill>};
