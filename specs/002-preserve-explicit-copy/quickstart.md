# Quickstart

Execute `node scripts/explicit-copy-e2e-test.js` e depois `npm test`. O teste deve retornar seis slides, zero chamadas de escrita, produto e CTA preservados e todos os destaques encontrados.
