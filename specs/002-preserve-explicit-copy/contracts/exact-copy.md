# Exact Copy Contract

Quando `copyMode` é `exact`, título e corpo são imutáveis durante geração, auditoria e recuperação. Apenas atributos visuais podem mudar. A quantidade final é a quantidade de blocos extraídos.
