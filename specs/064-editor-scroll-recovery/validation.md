# Validation

- `PLAYWRIGHT_MODULE=... node tests/campaign-review-browser.cjs` passed, including Edit → scroll to end → Review.
- `node dev/build-pages.cjs` and production verification will follow the local preview.
