# Validation

- `PLAYWRIGHT_MODULE=... node tests/campaign-review-browser.cjs` passed with single-header, tile and dialog-width assertions.
- Local preview ran at `http://localhost:8099/?view=overview` and was approved for deployment.
- Production verification will be recorded when GitHub Pages completes.
